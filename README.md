
  # Generate Concept Images

  This is a code bundle for Generate Concept Images. The original project is available at https://www.figma.com/design/kv2RNtei1h9q60nGwl0lwz/Generate-Concept-Images.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  